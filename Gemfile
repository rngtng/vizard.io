source 'https://rubygems.org'

ruby '2.2.3', :engine => 'jruby', :engine_version => '9.0.5.0'

gem 'haml'
gem 'sass'
gem 'sinatra'
gem 'dotenv'
# gem 'newrelic_rpm'
gem 'puma'
gem 'rack-timeout'

group :test do
  gem 'rspec'
  # gem 'ruby-debug'
end
